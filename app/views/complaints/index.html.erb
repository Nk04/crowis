<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" />
<%= render "shared/humburger" %>
<div class="mainView">
<%= render "shared/header" %>
<div class='main'>
  <div class='item-contents'>
    <%= link_to '新規投稿一覧', "#", class: "title" %>
    <ul class='item-lists'>
      <% @complaints.each do |complaints| %>
      <li class='list'>
        <%= link_to complaint_path(complaints.id) do %>
          <h1 class='item-name'>
            <%= complaints.title %>
          </h1>
          <p class='complaint-text'><%= complaints.text %></p>
        <% end %>
        <div class='star-btn'>
          <div id = "like-<%= complaints.id %>">
            <%= render  partial: 'shared/empathy', locals: { complaints: complaints } %>
          </div>
        </div>
      </li>
      <% end %>
    </ul>
  </div>
  <%= link_to  new_complaint_path do %>
    <div class='purchase-btn'>
      <span class='purchase-btn-text'>投稿する</span>
    </div>
  <% end %>
  <%= render "shared/footer" %>
</div>